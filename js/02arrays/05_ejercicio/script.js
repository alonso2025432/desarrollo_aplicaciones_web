console.log("conectado correctamente...");

/*
Enunciado:
Crea un arreglo con los días de
la semana. Luego, al hacer clic en
el botón, muestra con alert() el
día martes
usando su posición en el arreglo.
*/

function multiplicacindepares(){
    let numeros = [2,5,7,8,10];
    let resultado = (numeros[0]*numeros[2]*numeros[4]);
    alert("la multiplicacion de los numeros que estan en las pociciones pares es: \n" 
        +resultado);
}